<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Landlord Home -> Goes to Landlord Dashboard -->
    <item
        android:id="@+id/LandlordDashboardFragment"
        android:icon="@drawable/ic_home"
        android:title="@string/bottom_nav_home" />

    <!-- Landlord Dashboard -> Goes to Tenant Feed (Community) -->
    <item
        android:id="@+id/feedFragment"
        android:icon="@drawable/ic_grid"
        android:title="@string/bottom_nav_dashboard" />

    <!-- Profile stays the same -->
    <item
        android:id="@+id/profileFragment"
        android:icon="@drawable/ic_profile"
        android:title="@string/bottom_nav_profile" />


</menu>