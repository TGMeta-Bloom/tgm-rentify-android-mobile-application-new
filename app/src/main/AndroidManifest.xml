<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- REQUIRED PERMISSIONS -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_app_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_app_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.TGMRentify">
        <activity
            android:name=".view.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".view.OnboardingActivity" />

        <!-- FIX: Added adjustResize to LoginActivity to prevent layout panning -->
        <activity
            android:name=".view.LoginActivity"
            android:windowSoftInputMode="adjustResize"/>

        <activity android:name=".view.RoleSelectionActivity" />

        <!-- FIX: Added adjustResize to RegisterActivity to prevent layout panning -->
        <activity
            android:name=".view.RegisterActivity"
            android:windowSoftInputMode="adjustResize"/>

        <!-- Added Forgot Password Activities -->
        <activity android:name=".view.ForgotPasswordRequestActivity" />
        <activity android:name=".view.ForgotPasswordConfirmActivity" />

        <!-- Profile Section Activities -->
        <activity android:name=".view.ProfileInfoActivity" />
        <activity android:name=".view.AccountPrivacyActivity" />
        <activity android:name=".view.ChangePasswordActivity" />

        <!-- Account Delete Activity -->
        <activity android:name=".view.AccountDeleteActivity" />

        <!-- App Settings Activity -->
        <activity android:name=".view.AppSettingsActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
